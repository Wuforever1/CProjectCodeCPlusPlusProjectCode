#include<stdio.h>

int main()
{
    const int N = 2010;
    long long a[N] = {0};
    int n;
    long long c;
    scanf("%d %d", &n, &c);
    
    for(int i = 0; i < n; i++) scanf("%d", &a[i]);

    

    return 0;
}